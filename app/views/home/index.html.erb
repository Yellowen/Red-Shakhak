<%= javascript_include_tag  "endless_page" %>

<%= link_to_function "click here", "load_page(3)" %>

<div id="adbox">
    <% @advertises.each do |advertise| %>

      <div class="box box<%= advertise.get_size %> <%= advertise.category.cssclass %>">
	<div class="palette  innerbox">
	  <%= "#{advertise.id} -> #{advertise.cost_per_day}" %>
	</div>
      </div>

    <% end %>
</div>
